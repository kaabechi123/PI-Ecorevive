{% extends 'orgbase.html.twig' %}

{% block body %}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <div class="page-header">
        <h3 class="page-title">
            <span class="page-title-icon bg-gradient-primary text-white me-2">
                <i class="mdi mdi-calendar"></i>
            </span> Events
        </h3>
    </div>
    <div class="row">
        <div class="col-12 grid-margin">
            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th> Name </th>
                                    <th> Description </th>
                                    <th> Start Date </th>
                                    <th> End Date </th>
                                    <th> Location </th>
                                    <th> Organizer </th>
                                    <th> Delete </th>
                                    <th> Update </th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for event in events %}
                                    <tr>
                                        <td>{{ event.name }}</td>
                                        <td>{{ event.description }}</td>
                                        <td>{{ event.startDate|date('Y-m-d H:i') }}</td>
                                        <td>{{ event.endDate|date('Y-m-d H:i') }}</td>
                                        <td>{{ event.location }}</td>
                                        <td>{{ event.organizer.username }}</td>
                                        <td>
                                            <a onclick="showDeleteModal('{{ event.name }}', {{ event.id }})" class="badge badge-gradient-danger">Delete</a>
                                        </td>
                                        <td>
                                            <a href="{{ path('update_event2', {id: event.id}) }}" class="badge badge-gradient-info">Update</a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
        <!-- Add Event Button -->
    <div class="text-center mt-4">
        <a href="{{ path('create_event2') }}" class="btn btn-primary btn-circle btn-lg">
            <i class="fas fa-plus"></i>
        </a>
    </div>

    <!-- Delete Confirmation Modal -->
    <div id="deleteModal" class="modal fade" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirm Deletion</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p id="deleteMessage"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" onclick="closeModal()">No</button>
                    <button type="button" id="confirmDeleteButton" class="btn btn-danger">Yes, Delete</button>
                </div>
            </div>
        </div>
    </div>

<script>
    function showDeleteModal(eventName, eventId) {
        document.getElementById('deleteMessage').innerText = `Are you sure you want to delete ${eventName}?`;
        $('#deleteModal').modal('show');

        document.getElementById('confirmDeleteButton').onclick = function() {
            window.location.href = `/org/events/delete/${eventId}`;
        };
    }

    function closeModal() {
        $('#deleteModal').modal('hide');
    }
</script>
<style>
    .btn-circle {
        width: 60px;
        height: 60px;
        padding: 10px 16px;
        border-radius: 30px;
        font-size: 24px;
        line-height: 1.33;
        text-align: center;
    }

    .btn-circle.btn-lg {
        width: 70px;
        height: 70px;
        padding: 10px 16px;
        border-radius: 35px;
        font-size: 28px;
        line-height: 1.33;
    }
</style>
{% endblock %}
